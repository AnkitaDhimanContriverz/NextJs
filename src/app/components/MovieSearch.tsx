"use client";

import React, { useState } from "react";

export default function MovieSearch() {
  const [movie, setMovie] = useState([]);
  return (
    <div>
      <input type="text" />
    </div>
  );
}
